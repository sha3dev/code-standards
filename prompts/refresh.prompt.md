The scaffold, tooling, and AI contract for this repository were regenerated from `@sha3/code-standards`.

Before editing code:

- read `AGENTS.md` and `ai/contract.json`
- preserve the existing public contract unless the task explicitly allows breaking changes
- use any reference snapshot under `.code-standards/` when it exists
- realign the repository with the current standards instead of extending stale patterns
- keep managed files read-only unless this task is explicitly a standards update

Finish by reporting:

- changed files
- preserved contract decisions
- proof that `npm run check` passed

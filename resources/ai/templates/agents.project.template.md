# AI Coding Contract

Project: {{projectName}}
Contract format: `{{contractVersion}}`
Generated by `@sha3/code-standards`: `{{generatedByVersion}}`

`ai/contract.json` is the machine-readable source of truth for active rules. This file is the human-readable entrypoint.

## Active Profile

{{profileSummary}}

## Rule Hierarchy

1. `ai/contract.json`
2. `AGENTS.md`
3. `ai/*.md`
4. Local project configs (`package.json`, `biome.json`, `tsconfig.json`)
5. Existing source code and tests

If rules conflict, the higher item wins. Existing code has no grandfathered exceptions.

## Blocking Deterministic Rules

{{blockingRules}}

## Deterministic Checks Run By Tooling

- `npm run standards:check` validates project structure, README sections, managed files, metadata sync, and AI contract presence.
- `npm run lint` uses Biome for baseline linting and formatting rules.
- `npm run standards:check` also enforces AST-level project rules such as single return, async/await-only, section ordering, identifier quality, and naming rules.
- `npm run check` is the final blocking gate.

## Guidance Rules

{{guidanceRules}}

## Managed Files

{{managedFiles}}

Do not edit managed files during normal feature work unless the user explicitly requests a standards update. Use `npx @sha3/code-standards refactor --yes` when the package contract changes.

## Bootstrap Prompt

Before writing code:

1. Read `AGENTS.md`, `ai/contract.json`, and `ai/<assistant>.md`.
2. Summarize the blocking deterministic rules you will follow.
3. Implement the task without editing managed files.
4. Run `npm run check` and fix all issues before finishing.
